import random
jogos = int(input('Quantos jogos você quer gerar? '))
listajogo = []
jogototal = []
for c in range (1,jogos+1):
    for n in range(0,6):
        jogos = random.randint(0,60)
        listajogo.append(jogos)
    print(f'Jogo {c}: {listajogo}')
    listajogo.clear()
